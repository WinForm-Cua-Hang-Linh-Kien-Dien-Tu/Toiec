
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Admin_LayoutPage.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">

    <div class="form-group">
        @Html.ActionLink("Danh sách tài khoản", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
    </div>
    <div class="form-group">
        @Html.ActionLink("Danh sách từ vựng", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
        @Html.ActionLink("Danh sách chủ đề từ vựng", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
    </div>

    <div class="form-group">
        @Html.ActionLink("Danh sách bài giảng", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
        @Html.ActionLink("Danh sách bài tập", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
        @Html.ActionLink("Danh sách bài thi thử", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
    </div>
    <div class="form-group">
        @Html.ActionLink("Danh sách tin tức", "Create", "Travel", new { @class = "btn btn-primary btn-large" })
    </div>
</div>
}

